{
  "name": "emmons-brown-films-heroku",
  "private": true,
  "engines": {
    "node": "18.12.1"
  },
  "scripts": {
    "clean": "rm -rf public",
    "build": "npm install --prefix client && npm run build --prefix client",
    "deploy": "mkdir -p public && cp -a client/build/. public/",
    "heroku-postbuild": "npm run clean && npm run build && npm run deploy"
  }
}